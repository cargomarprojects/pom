

export class Addressm {
    add_pkid: string;
    add_parent_id: string;
    add_source: string;

    add_line1: string;
    add_line2: string;
    add_line3: string;
    add_line4: string;

    add_city_id: string;
    add_city_name: string;

    add_city: string;

    add_state_id: string;
    add_state_name: string;

    add_country_id: string;
    add_country_name: string;

    
    add_contact: string;
    add_location: string;

    add_pin: string;
    add_tel: string;
    add_fax: string;
    add_email: string;
    add_web: string;
    add_order: number;

    add_branch_slno: number;
    add_gstin: string;
    add_gst_type: string;

    add_sepz_unit: boolean;

    add_is_export : boolean ;

    add_default : string ;

    rec_mode: string;

}
